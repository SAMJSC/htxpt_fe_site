import styled from 'styled-components';

export const CommitPageContentContainer = styled.div`
  width: 100%;
  background-color: ${({ theme }) => theme.colors.white};
  min-height: 100vh;
  border-radius: 8px;
`;
