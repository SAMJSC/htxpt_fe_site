import React from 'react';

import { NewsContainer } from '@/app/components/atoms/News/styled';

const News = (): React.ReactElement => {
  return <NewsContainer>News</NewsContainer>;
};

export default News;
